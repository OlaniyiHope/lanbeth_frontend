import { useEffect, useMemo, useState } from "react"; import { Search, FileText, Eye, Check, Clock, ShieldCheck, ChevronRight, X, BookOpen, Download, } from "lucide-react"; import { useData } from "../../../context/DataContext.jsx"; import "./Policies.css"; function Policies() { const { data, getPolicies, getPolicy, markPolicyAsRead, } = useData(); const [search, setSearch] = useState(""); const [filter, setFilter] = useState("All"); const [selectedPolicy, setSelectedPolicy] = useState(null); const [loading, setLoading] = useState(false); const [opening, setOpening] = useState(false); /* |-------------------------------------------------------------------------- | POLICIES FROM DATABASE |-------------------------------------------------------------------------- */ const policies = Array.isArray(data?.policies) ? data.policies : []; /* |-------------------------------------------------------------------------- | LOAD POLICIES |-------------------------------------------------------------------------- */ useEffect(() => { const loadPolicies = async () => { try { setLoading(true); await getPolicies(); } catch (error) { console.error( "LOAD STAFF POLICIES ERROR:", error ); alert( error.message || "Failed to load policies." ); } finally { setLoading(false); } }; loadPolicies(); }, []); /* |-------------------------------------------------------------------------- | FILTER POLICIES |-------------------------------------------------------------------------- */ const filteredPolicies = useMemo(() => { return policies.filter((policy) => { const searchText = ` ${policy.title || ""} ${policy.policyType || ""} ${policy.description || ""} ${policy.fileName || ""} ${policy._id || ""} ${policy.status || ""} `.toLowerCase(); const matchesSearch = searchText.includes( search.toLowerCase() ); const matchesFilter = filter === "All" || policy.status === filter; return ( matchesSearch && matchesFilter ); }); }, [policies, search, filter]); /* |-------------------------------------------------------------------------- | SUMMARY |-------------------------------------------------------------------------- */ const totalPolicies = policies.length; const readPolicies = policies.filter( (policy) => policy.status === "Read" ).length; const unreadPolicies = policies.filter( (policy) => policy.status !== "Read" ).length; /* |-------------------------------------------------------------------------- | MARK AS READ |-------------------------------------------------------------------------- */ const markAsRead = async (id) => { try { await markPolicyAsRead(id); setSelectedPolicy((current) => { if ( !current || current._id !== id ) { return current; } return { ...current, status: "Read", }; }); } catch (error) { console.error( "MARK POLICY AS READ ERROR:", error ); alert( error.message || "Failed to mark policy as read." ); } }; /* |-------------------------------------------------------------------------- | OPEN POLICY |-------------------------------------------------------------------------- */ const openPolicy = async (policy) => { try { setOpening(true); /* * Get the latest policy from the backend. * This also gives us a fresh signed S3 URL. */ const result = await getPolicy( policy._id ); const latestPolicy = result?.policy || result; if (!latestPolicy) { throw new Error( "Policy could not be loaded." ); } setSelectedPolicy( latestPolicy ); /* * Mark as read when staff opens it. */ if ( latestPolicy.status !== "Read" ) { await markAsRead( latestPolicy._id ); } } catch (error) { console.error( "OPEN POLICY ERROR:", error ); alert( error.message || "Unable to open policy." ); } finally { setOpening(false); } }; /* |-------------------------------------------------------------------------- | OPEN PDF |-------------------------------------------------------------------------- */ const openDocument = async ( policy ) => { try { setOpening(true); const result = await getPolicy( policy._id ); const latestPolicy = result?.policy || result; if (!latestPolicy?.fileUrl) { throw new Error( "Secure document URL was not generated." ); } window.open( latestPolicy.fileUrl, "_blank", "noopener,noreferrer" ); /* * Reading the PDF also counts * as reading the policy. */ if ( latestPolicy.status !== "Read" ) { await markAsRead( latestPolicy._id ); } } catch (error) { console.error( "OPEN POLICY DOCUMENT ERROR:", error ); alert( error.message || "Unable to open policy document." ); } finally { setOpening(false); } }; /* |-------------------------------------------------------------------------- | FORMAT DATE |-------------------------------------------------------------------------- */ const formatDate = (date) => { if (!date) { return "—"; } return new Date( date ).toLocaleDateString( "en-GB", { day: "2-digit", month: "short", year: "numeric", } ); }; return ( <div className="staff-policies-page"> {/* ========================================================= HEADER ========================================================= */} <div className="page-head"> <div> <div className="eyebrow"> LANBETHCARE </div> <h1> Company Policies </h1> <p> Read company policies and keep up to date with important procedures and requirements. </p> </div> </div> {/* ========================================================= SUMMARY ========================================================= */} <div className="policy-summary"> <div className="policy-summary-card"> <div className="policy-summary-icon"> <FileText size={20} /> </div> <div> <small> Total Policies </small> <strong> {totalPolicies} </strong> </div> </div> <div className="policy-summary-card"> <div className="policy-summary-icon read"> <Check size={20} /> </div> <div> <small> Read </small> <strong> {readPolicies} </strong> </div> </div> <div className="policy-summary-card"> <div className="policy-summary-icon unread"> <Clock size={20} /> </div> <div> <small> Unread </small> <strong> {unreadPolicies} </strong> </div> </div> </div> {/* ========================================================= TOOLBAR ========================================================= */} <div className="policies-toolbar"> <div className="policy-search"> <Search size={16} /> <input type="text" placeholder="Search policies..." value={search} onChange={(e) => setSearch( e.target.value ) } /> </div> <div className="policy-filters"> <button className={ filter === "All" ? "active" : "" } onClick={() => setFilter("All") } > All </button> <button className={ filter === "Unread" ? "active" : "" } onClick={() => setFilter("Unread") } > Unread </button> <button className={ filter === "Read" ? "active" : "" } onClick={() => setFilter("Read") } > Read </button> </div> </div> {/* ========================================================= POLICY LIST ========================================================= */} <section className="policies-section"> <div className="section-title"> <div> <span className="eyebrow"> POLICY LIBRARY </span> <h2> Company Policies </h2> </div> <span className="policy-count"> {filteredPolicies.length}{" "} {filteredPolicies.length === 1 ? "policy" : "policies"} </span> </div> {loading ? ( <div className="policy-empty"> <div className="policy-empty-icon"> <FileText size={24} /> </div> <h3> Loading policies... </h3> <p> Please wait while the policy library is loaded. </p> </div> ) : filteredPolicies.length > 0 ? ( <div className="policy-list"> {filteredPolicies.map( (policy) => ( <div className={`policy-card ${ policy.status === "Unread" ? "policy-unread" : "" }`} key={policy._id} > {/* ICON */} <div className="policy-card-icon"> <FileText size={21} /> </div> {/* CONTENT */} <div className="policy-card-content"> <div className="policy-card-heading"> <div> <h3> {policy.title} </h3> <div className="policy-meta"> <span> {policy.policyType} </span> <span> Uploaded{" "} {formatDate( policy.createdAt )} </span> {policy.fileName && ( <span> PDF </span> )} </div> </div> <StatusBadge status={ policy.status || "Unread" } /> </div> <p> {policy.description || "No description provided."} </p> {/* FOOTER */} <div className="policy-card-footer"> <button className="outline small" onClick={() => openPolicy( policy ) } disabled={ opening } > <Eye size={14} /> {opening ? "Opening..." : "View Policy"} </button> {policy.status !== "Read" && ( <button className="mark-read-btn" onClick={() => markAsRead( policy._id ) } > <Check size={14} /> Mark as Read </button> )} <button className="policy-arrow" onClick={() => openPolicy( policy ) } aria-label="Open policy" > <ChevronRight size={17} /> </button> </div> </div> </div> ) )} </div> ) : ( <div className="policy-empty"> <div className="policy-empty-icon"> <FileText size={24} /> </div> <h3> {policies.length === 0 ? "No policies available" : "No policies found"} </h3> <p> {policies.length === 0 ? "There are currently no policies in the company policy library." : "Try changing your search or filter."} </p> </div> )} </section> <div className="watermark"> LAMBETH RESOLUTION HOMECARE </div> {/* ========================================================= POLICY VIEW MODAL ========================================================= */} {selectedPolicy && ( <PolicyModal policy={selectedPolicy} onClose={() => setSelectedPolicy(null) } onMarkRead={() => markAsRead( selectedPolicy._id ) } onOpenDocument={() => openDocument( selectedPolicy ) } /> )} </div> ); } /* ========================================================= STATUS BADGE ========================================================= */ function StatusBadge({ status, }) { const isRead = status === "Read"; return ( <span className={`policy-status ${ isRead ? "read" : "unread" }`} > <i /> {status} </span> ); } /* ========================================================= POLICY MODAL ========================================================= */ function PolicyModal({ policy, onClose, onMarkRead, onOpenDocument, }) { const isRead = policy.status === "Read"; return ( <div className="policy-modal-backdrop" onMouseDown={(e) => { if ( e.target === e.currentTarget ) { onClose(); } }} > <div className="policy-modal"> {/* HEADER */} <div className="policy-modal-header"> <div className="policy-modal-title"> <div className="policy-modal-icon"> <BookOpen size={20} /> </div> <div> <span className="eyebrow"> COMPANY POLICY </span> <h2> {policy.title} </h2> <div className="policy-modal-meta"> <span> {policy.policyType} </span> <span> Uploaded{" "} {formatDateValue( policy.createdAt )} </span> </div> </div> </div> <button className="policy-close" onClick={onClose} aria-label="Close policy" > <X size={18} /> </button> </div> {/* BODY */} <div className="policy-modal-body"> <div className="policy-notice"> <ShieldCheck size={17} /> <div> <strong> Please read this policy carefully. </strong> <span> Policies may contain important requirements relating to your role and responsibilities. </span> </div> </div> {/* DESCRIPTION */} <div className="policy-document"> <h3> {policy.title} </h3> <p> {policy.description || "No description provided."} </p> <div style={{ marginTop: "20px", padding: "16px", border: "1px solid #e5e7eb", borderRadius: "10px", }} > <div style={{ display: "flex", alignItems: "center", gap: "10px", }} > <FileText size={20} /> <div> <strong> {policy.fileName || "Policy document"} </strong> <div> <small> PDF document </small> </div> </div> </div> </div> </div> </div> {/* FOOTER */} <div className="policy-modal-footer"> <div> {isRead ? ( <span className="already-read"> <Check size={14} /> You have read this policy </span> ) : ( <span className="needs-reading"> <Clock size={14} /> This policy is unread </span> )} </div> <div className="policy-modal-actions"> <button className="outline" onClick={onClose} > Close </button> <button className="primary" onClick={onOpenDocument} > <Download size={15} /> Open PDF </button> {!isRead && ( <button className="primary" onClick={onMarkRead} > <Check size={15} /> Mark as Read </button> )} </div> </div> </div> </div> ); } /* ========================================================= DATE ========================================================= */ function formatDateValue(date) { if (!date) { return "—"; } return new Date( date ).toLocaleDateString( "en-GB", { day: "2-digit", month: "short", year: "numeric", } ); } export default Policies;